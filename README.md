# Accuoa.github.io

Personal hub at https://accuoa.github.io/. Astro+Tailwind, single page.

The "Currently exploring" list is sourced from [`src/data/projects.json`](src/data/projects.json) — entries are appended by each per-idea plan as that idea's repo goes live.

## Local dev

```
npm install
npm run dev
```
